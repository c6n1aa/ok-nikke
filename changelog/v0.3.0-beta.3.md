#### 新功能

- **input**: add synthetic touch pointer interaction backend
- **popups**: confirm the server selection screen during popup cleanup
- **event-calendar**: index FieldHubEvent and stop reviving stale bundled banners
- **event**: claim the mission popup per tab and skip locked STORY entries
- **event**: push the big-event story through the STORY sub-page
- **event**: add the mission popup flow
- **update**: read the local release notes of the new version and show them in the update-success card

#### 性能优化

- **navigation**: skip target-screen polls when the lobby is confirmed

#### 问题修复

- **input**: recover the synthetic touch worker after a crash and report its real state
- **cash-shop**: tell the limited-time and the ordinary package pages apart so the free-package task stops on the right one
- **harvest**: judge the PASS modal by its tab labels and click the tab boxes directly
- **event**: retry the challenge-stage click with a randomized offset and tune the list scrolling

- 本次更新包含入口程序变更，应用内更新无法覆盖，请重新下载完整便携包。
