#### 新增
- 「关于 → 应用更新」新增依赖镜像源选择（自动 / 官方 PyPI / 清华 / 阿里云 / 腾讯云），与更新源并列显示；国内用户默认「自动」走国内镜像，更新时重装依赖更快更稳。

#### 优化
- 「应用更新源」与「Pip 镜像源」标签已区分，不再混淆。
- 依赖管理迁移到 uv，锁文件更精确（开发者侧；日常使用无感知）。

#### 说明
- 本次更新后首次启动会触发一次依赖重装（依赖清单格式微调所致），约需几分钟，期间请勿关机或结束进程。
- 无需重新下载便携包：本次未改动入口程序与包结构，应用内更新即可。
- 如需提前备份日常配置：先退出应用，把包目录下的 `configs` 整个文件夹复制到其它位置备份；恢复时把备份的 `configs` 放回包目录即可。